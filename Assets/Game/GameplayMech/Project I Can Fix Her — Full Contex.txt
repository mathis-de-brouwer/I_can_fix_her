Project: I Can Fix Her — Full Context Analysis (Updated)

Genre & concept
A local 2-player asymmetric PvP game. The two players have completely different gameplay loops that work against each other in real time on the same machine:
• P1 (the Survivor) — plays like Vampire Survivors: top-down 2D, moves around an infinite procedurally generated world, auto-attacks enemies, gains XP, levels up, gets stronger through passive items and weapons
• P2 (the Antagonist) — plays like Slay the Spire: manages a hand of cards, spends charges to play them, using card effects to make P1's life harder
• Win condition: P1 dies -> P2 wins. P2's deck runs out -> P1 wins (but see Final Boss rule below)

---

P1 — The Survivor system

CharacterSciptableObject (SO)
  └── base stats: maxHealth, recovery, moveSpeed, might, projectileSpeed, magnet
        └── loaded into PlayerStats on Awake as currentX fields

Playermovement
  ├── WASD/stick input via InputAction
  ├── dash system (cooldown, I-frames, layer ignore during dash)
  └── reads playerStats.currentMovementSpeed at runtime

AimController
  ├── right stick (gamepad) or IJKL (keyboard)
  └── exposes AimDirection — falls back to lastMoveDirection when no aim input

Weapon system (important prefab split)
Weapons are represented by TWO kinds of prefabs:
1) Weapon Controller Prefab (equipped under the player)
   ├── has a WeaponController subclass (e.g. KnifeController : WeaponController)
   ├── has weaponData assigned (WeaponScriptableObject)
   └── lives as a child of the player and auto-fires on cooldown

2) Projectile Prefab (spawned when attacking)
   ├── referenced by WeaponScriptableObject.Prefab
   ├── has projectile behavior scripts (e.g. KnifeBehavior)
   └── does NOT contain WeaponController
   └── NOTE: some weapons spawn a projectile that later spawns an AoE "explosion" prefab which applies damage via a trigger zone (ExplosionDamageZone).

WeaponController (base)
  ├── auto-fires on cooldown
  ├── reads AimController.AimDirection via GetAimDirection()
  └── derived controllers override Attack() to spawn projectiles (weaponData.Prefab)

WeaponScriptableObject (data)
  ├── Visuals: Icon, WeaponName
  ├── Stats: Damage, Speed, CooldownDuration, Pierce, Level
  ├── Prefab: projectile prefab
  └── NextLevelPrefab: the NEXT *controller prefab* (for inventory upgrades)

Passive items
PassiveItems (base MonoBehaviour on prefabs)
  ├── holds passiveItemsData (PassiveItemsScriptableObjects)
  └── Start() calls ApplyModifier() once (permanent modifier applied immediately)

PassiveItemsScriptableObjects (data)
  ├── Visuals: Icon, ItemName
  ├── Stats: Multiplier, Level
  ├── Identity: BaseItem (used to treat multiple levels as one item line)
  └── NextLevelPrefab: next passive prefab for upgrades

InventoryManager
  ├── WeaponSlots[6] : List<WeaponController>
  ├── PassiveItemSlots[6] : List<PassiveItems>
  ├── weaponLevels[6], passiveItemLevels[6]
  ├── Awake() ensures slot lists have real Count==6 (capacity != count)
  ├── AddWeapon(slot, weapon) validates WeaponController + weaponData
  ├── LevelUpWeapon(slot) replaces controller prefab with weaponData.NextLevelPrefab
  └── LevelUpPassiveItem(slot) replaces passive prefab with passiveItemsData.NextLevelPrefab

PlayerStats
  ├── IncreaseExperience() -> LevelUpChecker() -> NotifyLevelUp(level)
  ├── XP progression uses a scaling function (growth factor + flat bonus per level) to increase experienceCap
  ├── RegisterStartingInventory():
  │     - scans child WeaponControllers/PassiveItems at runtime
  │     - registers them into InventoryManager slot lists
  │     - sets weaponIndex/passiveItemIndex to next empty slot
  ├── SpawnWeapon(controllerPrefab):
  │     - instantiates controller prefab as child of player
  │     - registers into first free slot index (tracked by weaponIndex)
  └── SpawnPassiveItem(passivePrefab):
        - instantiates passive item prefab as child of player
        - registers into first free slot index (tracked by passiveItemIndex)

---


P2 — The Antagonist system

P2DeckSelectionSession (DontDestroyOnLoad singleton)
  └── carries P2DeckBuildConfig from menu scene -> gameplay scene

P2DeckManager
  ├── deck / hand / usedPile — List<P2Card>
  ├── P2Charges — float resource, regenerates over time, spent to play cards
  ├── TryPlayCard() -> ResolvePlayRoutine() -> P2CardEffectResolver.Resolve()
  ├── Lose condition: normally, deck empty + hand empty -> GameResultScreenUI.Setup(Winner.P1)
  └── NEW: Final Boss rule (see below) modifies the deck-empty lose flow

P2Card (ScriptableObject)
  ├── cost (charges), duration, value, prefab
  └── effect (P2CardEffect SO) — defines what the card does

P2CardEffect subclasses:
  ├── P2DoubleMonstersCardEffect  — duplicates N random live enemies
  ├── P2SpawnWaveDirectionalCardEffect — spawns a wave of enemies from one direction toward P1
  ├── P2SpawnSurgeCardEffect — spawns a large surge over time around the player
  └── NEW: P2SpawnBossCardEffect — spawns exactly one boss enemy and attaches a death hook (below)

P2CardEffectResolver
  └── builds P2CardEffectContext (spawnPoint, charges, target = P1 transform, elapsedSeconds from P2MatchClock, timeScaling) -> card.effect.Resolve()

P2DeckVisualUI
  └── listens to DeckStateChanged event -> updates deck/discard count display in real time

P2HandUI / P2CardUI
  └── hand UI is driven by P2DeckManager.hand; special boss card is displayed like any other card when granted

NEW: Final Boss rule (P2 “last stand”)
  Goal: When P2 runs out of cards, they do not instantly lose. Instead they get one final boss card.
  ├── Trigger: deck.Count == 0 AND hand.Count == 0 (after _gameStarted)
  ├── Action: P2DeckManager grants a configured bossCard into hand once (bossCard has cost 0)
  ├── P2 must play the boss card (normal UI click -> TryPlayCard)
  ├── Boss card uses P2SpawnBossCardEffect:
  │     - spawns card.prefab once near the player
  │     - calls P2DeckManager.NotifyBossSpawned()
  │     - attaches P2BossDeathHook to the spawned boss
  └── Boss death:
        - P2BossDeathHook.OnDestroy() calls P2DeckManager.NotifyBossDied()
        - P2DeckManager ends the match with Winner.P1

Files added/changed for Final Boss rule
  ├── CHANGED: Assets/Game/GameplayMech/Player2/Scripts/DeckLogic/P2DeckManager.cs
  │     - added bossCard reference + boss phase flags
  │     - modified CheckDeckEmpty() to grant boss card once, then use boss state
  ├── ADDED: Assets/Game/GameplayMech/Player2/Scripts/CardEffect/P2SpawnBossCardEffect.cs
  └── ADDED: Assets/Game/GameplayMech/Player2/Scripts/CardEffect/P2BossDeathHook.cs

---

The world

MapController
  └── infinite procedural chunk-based world — spawns/culls terrain chunks around the camera
  └── spawns both terrain chunks and chunk groups
  └── NOTE: repeated console logs about "AnimationEvent has no function name specified" are caused by animation clips on spawned props (e.g. AutumnTree) containing a blank Animation Event.

Ysorting
  └── dynamic sortingOrder based on Y position — gives pseudo-depth to 2D sprites

EnemyStats
  ├── health, damage, knockback, hit flash, stagger
  ├── OnCollisionStay2D -> PlayerStats.TakeDamage()
  └── Kill() -> DropRateManager.TriggerDrop() -> spawns XP gem / items at death position

EnemyMovement
  └── moves toward P1 using EnemyStats.GetMoveSpeed(), respects stagger state

DropRateManager
  └── weighted random drop table per enemy — drops prefabs (XP gems etc.) at enemy position

NEW: Continuous enemy spawning - “Vampire Survivors” style
  Goal: Spawn enemies continuously and ramp difficulty over time with an AnimationCurve-based multiplier.

  Core ideas:
  ├── Reuses weighted spawn tables via P2SpawnEntry + SpawnTable.Pick()
  ├── Uses P2MatchClock.ElapsedSeconds and P2TimeScalingConfig.GetMagnitudeMultiplier(t)
  ├── Spawns enemies in a ring around the player (radius + jitter)
  ├── Enforces a max-alive cap to avoid runaway spawning
  └── Tracks alive enemies using a registry instead of expensive scene searches

  New/added components:
  ├── EnemySpawnDirector (MonoBehaviour)
  │     - reads elapsedSeconds
  │     - computes spawnsPerSecond and maxAlive using curve multiplier
  │     - spawns prefabs on a timed loop (accumulator + interval)
  │     - registers spawned enemies in EnemySpawnRegistry
  ├── EnemySpawnDirectorConfig (ScriptableObject)
  │     - spawnTable: List<P2SpawnEntry>
  │     - baseSpawnsPerSecond, baseMaxAlive
  │     - spawnRadius, randomJitter
  │     - timeScaling: P2TimeScalingConfig (curve-driven)
  │     - spawnsPerSecondScale, maxAliveScale
  └── EnemySpawnRegistry (static)
        - maintains AliveCount
        - Register(enemy) auto-attaches a hook component that decrements on OnDestroy()

Files added/changed for continuous spawning
  ├── ADDED: Assets/Game/GameplayMech/Enemies/Spawning/EnemySpawnDirector.cs
  ├── ADDED: Assets/Game/GameplayMech/Enemies/Spawning/EnemySpawnDirectorConfig.cs
  └── ADDED: Assets/Game/GameplayMech/Enemies/Spawning/EnemySpawnRegistry.cs

---

Level-up reward system (updated: mixed weapon/passive/upgrade offers)

LevelUpRewardManager (scene singleton)
  └── fires every N P1 levels (levelsPerReward)
  └── pauses the game:
       - disables P1 gameplay inputs
       - Time.timeScale = 0
  └── spawns reward UI prefab (LevelUpRewardScreen)
  └── P1 offers are generated as a mix (3 slots):
       - NewPassive (spawn passive prefab)
       - NewWeapon (spawn weapon controller prefab)
       - PassiveUpgrade (upgrade a specific passive slot)
       - WeaponUpgrade (upgrade a specific weapon slot)
  └── Offer generation is inventory-aware:
       - computes upgradeable slots by checking NextLevelPrefab != null
       - filters new items by excluding items already owned (by data reference)
       - debug logging available via debugOffers toggle

P1RewardOffer (unified offer model)
  ├── Type: NewPassive / NewWeapon / PassiveUpgrade / WeaponUpgrade
  ├── Prefab: display prefab (for new offers, this is the prefab to spawn;
  │           for upgrades, this is the currently equipped object for UI display)
  └── SlotIndex: for upgrades

LevelUpRewardScreenUI
  ├── left panel: P1ItemChoiceUI (controller navigation)
  ├── right panel: P2CardChoiceUI
  └── screen closes only when BOTH players have confirmed:
       - On close: LevelUpRewardManager.OnRewardScreenClosed()
       - resumes time and restores inputs
       - destroys the reward screen prefab

P1ItemChoiceUI / P1ItemOptionUI
  ├── displays offers based on type:
       - passives use PassiveItemsScriptableObjects (icon/name/multiplier)
       - weapons use WeaponScriptableObject (icon/weaponName/level)
       - upgrades show Upgrade: <name> and next level info when possible
  └── locks confirmation so only one P1 selection is accepted

P2CardChoiceUI
  └── should lock confirmation after first click (prevents multi-select)

---

UI Tooltips (updated: tooltip “channels” / multiple controllers)

Goal
  Allow multiple independent tooltips at the same time (e.g., Level-up screen:
  P1 inspect tooltip on the left, P2 inspect tooltip on the right) without UI cross-talk.

Core idea
  Tooltip routing is now controller-based:
  - UI elements can optionally reference a specific TooltipController (“channel”).
  - If no specific controller is assigned, they fall back to TooltipController.Instance
    (legacy/default behavior).

Key components
  TooltipController (MonoBehaviour)
    - No longer enforces a single instance by destroying duplicates.
    - First TooltipController that Awakes becomes TooltipController.Instance (default).
    - Multiple TooltipControllers can coexist in the same scene/prefab and be targeted directly.

  HoverTooltipSource (MonoBehaviour)
    - Has an optional TooltipController reference (Routing).
    - Exposes SetController(TooltipController).
    - Uses the injected controller when provided, otherwise uses TooltipController.Instance.

  P1 item picker tooltips (controller inspect)
    - P1ItemOptionUI now has a serialized TooltipController reference for the P1 channel.
    - P1ItemChoiceNavigator shows/hides tooltips using the selected option’s assigned controller
      (fallback: TooltipController.Instance).

  P2 card picker tooltips (hover/right-click)
    - P2CardOptionUI now has a serialized TooltipController reference for the P2 channel.
    - On Setup(), it injects that controller into HoverTooltipSource via SetController().

Files changed for tooltip channels
  ├── CHANGED: Assets/Game/GameplayMech/UI/Tooltip/Scripts/TooltipController.cs
  │     - removed “destroy duplicate singleton” behavior; first instance becomes default only
  ├── CHANGED: Assets/Game/GameplayMech/UI/Tooltip/Scripts/HoverTooltipSource.cs
  │     - added optional routing controller + SetController(); fallback to Instance remains
  ├── CHANGED: Assets/Game/GameplayMech/UI/Itemscreen/Scripts/P1ItemOptionUI.cs
  │     - added TooltipController reference (P1 channel) exposed via property
  ├── CHANGED: Assets/Game/GameplayMech/UI/Itemscreen/Scripts/P1ItemChoiceNavigator.cs
  │     - routes P1 inspect tooltip to the selected option’s TooltipController
  └── CHANGED: Assets/Game/GameplayMech/UI/Itemscreen/Scripts/P2CardOptionUI.cs
        - added TooltipController reference (P2 channel) and injects it into HoverTooltipSource

Recent changes (implemented)

Pickups / magnet (fix “yeeted collectibles”)
- PlayerCollector no longer collects instantly on magnet trigger.
- PickUp now supports attraction:
  - PlayerCollector.OnTriggerEnter2D() calls PickUp.BeginFollow(playerTransform) for ICollectible items.
  - PickUp.FixedUpdate() steers the Rigidbody2D toward the follow target (followSpeed/maxFollowSpeed).
  - Collect() is invoked only when the pickup hits the Player trigger; then the pickup is destroyed.
Files changed:
- Assets/Game/GameplayMech/Player/PlayerCollector.cs
- Assets/Game/GameplayMech/Pick-ups/PickUp.cs

XP progression (less exponential)
- PlayerStats XP cap no longer uses a multiplicative growth factor (fields commented out).
- XP cap increases are now driven by LevelRanges (linear-by-segment):
  - PlayerStats.CalculateNextExperienceCap() adds the matching range’s experienceCapIncrease each level.
File changed:
- Assets/Game/GameplayMech/Player/PlayerStats.cs

Weapon level-up behaviors
- Chakram (Orbit):
  - OrbitController spawns multiple orbit prefabs based on weaponData.Level * orbitCountPerLevel.
  - OrbitBehavior supports InitializeOrbit(index, total) to apply angle offsets so orbits are equally spaced.
Files changed:
- Assets/Game/GameplayMech/Weapon/Scripts/Weapon Controller/OrbitController.cs
- Assets/Game/GameplayMech/Weapon/Scripts/Weapon Behavior/OrbitBehavior.cs

- Knife:
  - KnifeController supports multi-shot when weaponData.Level >= multiShotLevel (default 3),
    spawning multiShotCount knives with a configurable spread.
File changed:
- Assets/Game/GameplayMech/Weapon/Scripts/Weapon Controller/Knife Controller.cs

- Shield:
  - ShieldController scales ShieldBehavior.maxShieldHealth up and shieldCooldown down per weapon level
    (clamped by minShieldCooldown).
File changed:
- Assets/Game/GameplayMech/Weapon/Scripts/Weapon Controller/ShieldController.cs

NEW: Magic Staff (random-direction fireball + AoE explosion)
- MagicStaffController:
  - Spawns a staff visual that floats next to the player (bobbing only; no rotation).
  - Fires fireballs automatically on cooldown using weaponData.Prefab.
  - Fire direction is random (4-way; optional diagonals).
  - Fireball spawns offset away from the player in its travel direction (spawnDistance) to avoid overlap.
  - Explosion radius scales with weapon level:
    radius = baseExplosionRadius + explosionRadiusPerLevel * (weaponData.Level - 1)
- FireballBehavior (projectile):
  - Moves forward in the initialized direction.
  - Explodes on enemy hit OR on lifetime expiry (whichever happens first).
  - Rotates to face its travel direction.
  - Spawns an explosion prefab oriented to the travel direction.
  - Plays SFX: cast + explosion.
- ExplosionDamageZone (explosion prefab behavior):
  - Explosion prefab has a CircleCollider2D trigger that applies AoE damage to enemies detected via EnemyStats in parent.
  - Radius is set at runtime by adjusting CircleCollider2D.radius (supports level scaling).
  - Lifetime is controlled by lifetimeSeconds (destroys itself).
Files added/changed:
- ADDED/CHANGED: Assets/Game/GameplayMech/Weapon/Scripts/Weapon Controller/MagicStaffController.cs
- ADDED/CHANGED: Assets/Game/GameplayMech/Weapon/Scripts/Weapon Behavior/FireballBehavior.cs
- ADDED/CHANGED: Assets/Game/GameplayMech/Weapon/Scripts/Weapon Behavior/ExplosionDamageZone.cs
- CHANGED: Assets/Game/GameplayMech/Audio/UiSfx.cs
  - added sfxFireballCast / sfxFireballExplode + PlayFireballCast() / PlayFireballExplode()

Level-up reward offers (sequential level gating)
- NewWeapon/NewPassive offers are filtered to only include items with Level == 1.
  Higher level prefabs should only be reachable via upgrade offers (NextLevelPrefab chain).
File changed:
- Assets/Game/GameplayMech/UI/Itemscreen/LevelUpRewardManager.cs

Deckbuilder tooltip (pinned/controller-routed tooltips)
- Deckbuilder card tile now populates HoverTooltipSource content from P2Card (icon/title/description).
- Deckbuilder passes a TooltipController reference into tiles for explicit routing (supports fixed anchor usage).
Files changed:
- Assets/Game/GameplayMech/Player2/Scripts/DeckLogic/UI/P2DeckBuilderUI.cs
- Assets/Game/GameplayMech/Player2/Scripts/DeckLogic/UI/P2DeckBuilderCardTileUI.cs





